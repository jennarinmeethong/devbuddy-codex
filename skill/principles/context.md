# Context Management

Maintain project memory incrementally.

Update only relevant sections, preserve existing knowledge, mark assumptions, and record unknowns. Do not overwrite unrelated information.

When memory is stored in an Obsidian vault, cross-link related notes with `[[wikilinks]]` so the knowledge stays navigable.

