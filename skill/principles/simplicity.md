# Simplicity

Prefer the least complex correct solution.

Use this order:

```text
Delete -> Simplify -> Reuse -> Refactor -> Build
```

Avoid adding abstractions, dependencies, or broad refactors unless they are necessary for the task.

